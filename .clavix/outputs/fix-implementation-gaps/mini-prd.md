# Requirements: Fix Implementation Gaps

_Generated from conversation on Thu Nov 27 2025_

## Objective

Systematically audit the entire 10x-mapmaster codebase against `docs/architecture/` specifications and create granular OpenSpec change proposals to fix all implementation gaps where agents marked work complete without proper adherence to the documented design.

## Core Requirements

### Must Have (High Priority)

- [HIGH] **Phase 1: Parallel Audit** - Run 3 code-reviewer subagents simultaneously:
  - **Agent 1 (Database)**: Audit `supabase/db/` against `algorithm.md`, `architecture.md`, `gameplay.md`
  - **Agent 2 (Edge Functions)**: Audit `supabase/functions/` against `algorithm.md`, `operations.md`
  - **Agent 3 (Frontend UI)**: Audit `src/` (skip game store internals, focus on UI components and how store methods are used) against `ui.md`, `gameplay.md`

- [HIGH] **Audit Output Format**: Each agent produces a gap list organized by doc section containing:
  - 1-2 sentence explanation of what's missing
  - Suggestion for how to fix
  - File:line references where applicable

- [HIGH] **Phase 2: OpenSpec Changes** - Create very granular change proposals:
  - Naming convention: `01-fix-...`, `02-fix-...`, etc.
  - Verb-led, kebab-case per AGENTS.md
  - Each change includes: `proposal.md`, `tasks.md`, spec deltas
  - Validate with `openspec validate [change] --strict`

- [HIGH] **Execution Order**: Complete ALL backend fixes before touching frontend
  - Backend is critical foundation
  - Don't double work by fixing frontend on broken backend

### Specific Technical Fixes Expected

- [HIGH] **LLM Question Translation**: Wire up `call-llm` edge function for question translation
  - Current (wrong): `get_llm_question` generates questions via LLM
  - Correct: Algorithm selects `geographic_region/trait` → LLM translates to natural language
  - Remove string templates like "Does it have"

- [HIGH] **UI Map**: Change from flat map to globe
- [HIGH] **UI Markers**: Implement 3D markers instead of flat markers

## Technical Constraints

- **Framework/Stack**: Vue 3 frontend, Supabase backend (PostgreSQL + Edge Functions)
- **Architecture**: Database-first - ALL business logic in PostgreSQL
- **Workflow**: OpenSpec for all changes (not direct fixes)
- **Validation**: `openspec validate [change] --strict` for every change proposal

## User Context

**Target Users:** Development team fixing implementation gaps
**Primary Use Case:** Ensure codebase matches documented architecture
**Workflow:**

1. Run parallel audit
2. Review gap lists
3. Create OpenSpec changes for backend gaps
4. Implement backend fixes
5. Then audit/fix frontend

## Edge Cases & Considerations

- Game store internals should be skipped during frontend audit (focus on UI usage of store methods)
- Current implementation is partially working - can play games via SQL and browser
- Algorithm exists but doesn't work as designed in docs

## Implicit Requirements (v4.4)

_Inferred from conversation context - please verify:_

- [Infrastructure] Edge function `call-llm` exists but needs to be properly integrated
- [Quality] Avoid string templates where possible per docs
- [Process] Each OpenSpec change should be small enough to review/implement independently

> **Note:** These requirements were surfaced by analyzing conversation patterns.

## Success Criteria

How we know this is complete and working:

- ✓ All 5 architecture docs have been audited against code
- ✓ Gap list produced for each doc section
- ✓ OpenSpec changes created for every identified gap
- ✓ All changes validate with `--strict` flag
- ✓ Backend fixes implemented before frontend work begins
- ✓ LLM translates questions to natural language (no templates)
- ✓ Map displays as globe with 3D markers
- ✓ Algorithm works as designed in `algorithm.md`

## Next Steps

1. Review this PRD for accuracy and completeness
2. Run the 3-agent parallel audit
3. Create OpenSpec changes from audit results
4. Implement backend fixes first
5. Then proceed to frontend fixes

---

_This PRD was generated by Clavix from conversational requirements gathering._
