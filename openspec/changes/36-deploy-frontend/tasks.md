# Tasks: Add Deploy Frontend

- [ ] Configure Pages (or equivalent) for static hosting
- [ ] Workflow: build frontend (bun run build) after lint/tests succeed; upload artifact; deploy to Pages
- [ ] Set environment variables for build (Supabase URL/anon key, etc.)
- [ ] Protect main: deploy only on main or tagged releases per policy
- [ ] Validate `openspec validate 36-deploy-frontend --strict`
