## REMOVED Requirements

### Requirement: Store Mutation Pattern

REMOVED - This requirement existed only to support realtime subscriptions which are being removed. The general principle of mutating state through actions is standard Pinia practice and doesn't need explicit specification.

The following scenarios are removed:

- Scenario: Adding a place via realtime
- Scenario: Updating a place via realtime
- Scenario: Removing a place via realtime
